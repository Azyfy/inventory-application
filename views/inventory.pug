extends layout

block content
  h1= title

  div(class="d-flex flex-column")
    a(href="/inventory") 
      button Inventory
    a(href="/") 
      button back
    a(href="/inventory/cars") 
      button Cars

  if error
    p Error getting dynamic content.
  else
    p The inventory has the following record counts:

    ul
      li #[strong Cars:] !{data.car_count}
      li #[strong Manufacturer:] !{data.manufacturer_count}
      li #[strong Categories:] !{data.category_count}