extends inventory

block content

    h1= title 

    ul
        each car in car_list
            li 
                a(href=car.url) #{car.model} 
                | - #{car.manufacturer.name}
        
        else 
            li There are no cars